"""AUTO-GENERATED by codegen/generate.py. DO NOT EDIT BY HAND.

Regenerate with: python codegen/generate.py
Sources: committed discovery documents under codegen/discovery/ plus the
Developer Preview overlay codegen/overlay/docs_preview_overlay.json.
"""
# Google Docs API v1 Developer Preview: batchUpdate Request-union members
# sourced from codegen/overlay/docs_preview_overlay.json (absent from
# public discovery until preview enrollment).

import asyncio
import json
from typing import Any, Optional

from mcp.types import ToolAnnotations

from auth.service_decorator import require_google_service
from core.server import server
from core.utils import JsonDict, handle_http_errors


@server.tool(
    title="Docs API Preview: acceptSuggestion (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=False,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_accept_suggestion", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_accept_suggestion(
    service: Any,
    user_google_email: str,
    document_id: str,
    suggestion_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request acceptSuggestion.

    Accepts a suggestion. Returns a 403 forbidden error if the requesting user does not
    have edit access to the document.

    Auto-generated wrapper that sends a single AcceptSuggestionRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        suggestion_id (str): The ID of the suggestion.

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
    """
    request_body: dict[str, Any] = {}
    if suggestion_id is not None:
        request_body["suggestionId"] = suggestion_id
    body: dict[str, Any] = {"requests": [{"acceptSuggestion": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: addCommentReply (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=False,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_add_comment_reply", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_add_comment_reply(
    service: Any,
    user_google_email: str,
    document_id: str,
    comment_id: Optional[str] = None,
    post: Optional[JsonDict] = None,
    suggestion_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request addCommentReply.

    Inserts a reply Post into a CommentThread or SuggestionThread. Exactly one of
    commentId or suggestionId must be set (thread_id union).

    Auto-generated wrapper that sends a single AddCommentReplyRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        comment_id (str): The ID of the CommentThread to add the reply to (thread_id
            union member).
        post (dict): The Post representing the reply. Object schema Post: {assigneeEmail
            (string), author (PostAuthor): Output only., commentAction (string), content
            (string), contentHtml (string), createTime (string), deleted (boolean),
            fromCopiedDocument (boolean), fromDocumentComparison (boolean),
            fromImportedDocument (boolean), postId (string): Output only.,
            suggestionAction (string), updateTime (string)}.
        suggestion_id (str): The ID of the SuggestionThread to add the reply to
            (thread_id union member).

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
        The reply for this request (in replies[0]) is AddCommentReplyResponse: post
            (Post): The newly-inserted reply Post..
        Post fields: assigneeEmail (string): The email address of the assignee of the
            comment.; author (PostAuthor): Output only.; commentAction (string): The
            action taken with this reply to a comment thread.; content (string): The
            content of the post.; contentHtml (string): Output only.; createTime
            (string): Output only.; deleted (boolean): Output only.; fromCopiedDocument
            (boolean): Output only.; fromDocumentComparison (boolean): Output only.;
            fromImportedDocument (boolean): Output only.; postId (string): Output only.;
            suggestionAction (string): The action taken with this reply to a suggestion
            thread.; updateTime (string): Output only..
    """
    request_body: dict[str, Any] = {}
    if comment_id is not None:
        request_body["commentId"] = comment_id
    if post is not None:
        request_body["post"] = post
    if suggestion_id is not None:
        request_body["suggestionId"] = suggestion_id
    body: dict[str, Any] = {"requests": [{"addCommentReply": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: deleteComment (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=True,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_delete_comment", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_delete_comment(
    service: Any,
    user_google_email: str,
    document_id: str,
    comment_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request deleteComment.

    Deletes a CommentThread. Returns a 400 bad request error if the requesting user is
    not the author of the headPost.

    Auto-generated wrapper that sends a single DeleteCommentRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        comment_id (str): The ID of the CommentThread that is being deleted.

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
    """
    request_body: dict[str, Any] = {}
    if comment_id is not None:
        request_body["commentId"] = comment_id
    body: dict[str, Any] = {"requests": [{"deleteComment": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: deleteCommentReply (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=True,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_delete_comment_reply", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_delete_comment_reply(
    service: Any,
    user_google_email: str,
    document_id: str,
    comment_id: Optional[str] = None,
    post_id: Optional[str] = None,
    suggestion_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request deleteCommentReply.

    Deletes a reply Post from a CommentThread or SuggestionThread. Returns a 400 bad
    request error if the requesting user is not the author of the post, if the reply
    post contains an action, or if the reply post contains an assignee. Exactly one of
    commentId or suggestionId must be set (thread_id union).

    Auto-generated wrapper that sends a single DeleteCommentReplyRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        comment_id (str): The ID of the CommentThread which the post belongs to
            (thread_id union member).
        post_id (str): The ID of the reply Post being deleted.
        suggestion_id (str): The ID of the SuggestionThread which the post belongs to
            (thread_id union member).

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
    """
    request_body: dict[str, Any] = {}
    if comment_id is not None:
        request_body["commentId"] = comment_id
    if post_id is not None:
        request_body["postId"] = post_id
    if suggestion_id is not None:
        request_body["suggestionId"] = suggestion_id
    body: dict[str, Any] = {"requests": [{"deleteCommentReply": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: deleteSuggestion (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=True,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_delete_suggestion", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_delete_suggestion(
    service: Any,
    user_google_email: str,
    document_id: str,
    suggestion_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request deleteSuggestion.

    Deletes a suggestion. Returns a 403 forbidden error if the requesting user is not
    the author of the suggestion.

    Auto-generated wrapper that sends a single DeleteSuggestionRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        suggestion_id (str): The ID of the suggestion.

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
    """
    request_body: dict[str, Any] = {}
    if suggestion_id is not None:
        request_body["suggestionId"] = suggestion_id
    body: dict[str, Any] = {"requests": [{"deleteSuggestion": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: insertComment (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=False,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_insert_comment", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_insert_comment(
    service: Any,
    user_google_email: str,
    document_id: str,
    assignee_email_address: Optional[str] = None,
    content: Optional[str] = None,
    range: Optional[JsonDict] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request insertComment.

    Inserts a CommentThread into the document. The comment is authored as the
    authenticated user.

    Auto-generated wrapper that sends a single InsertCommentRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        assignee_email_address (str): Optional.
        content (str): The text of the comment, as plain text.
        range (dict): The Range in the document that is tied to this comment (member of
            the 'anchor' union; currently the only member). Object schema Range:
            {endIndex (integer), segmentId (string), startIndex (integer), tabId
            (string)}.

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
        The reply for this request (in replies[0]) is InsertCommentResponse:
            commentThread (CommentThread): The newly-inserted comment thread..
        CommentThread fields: anchorId (string): The ID of the CommentAnchor in the
            document that this thread is tied to.; commentId (string): The unique ID of
            the comment thread.; headPost (Post): The first post in the thread.;
            plainTextQuote (string): The quoted text from the document when the comment
            was created, formatted as plain-text (member of the 'quote' union).; replies
            (array<Post>): Replies to the head post.; status (string): Whether the
            thread is open or resolved..
    """
    request_body: dict[str, Any] = {}
    if assignee_email_address is not None:
        request_body["assigneeEmailAddress"] = assignee_email_address
    if content is not None:
        request_body["content"] = content
    if range is not None:
        request_body["range"] = range
    body: dict[str, Any] = {"requests": [{"insertComment": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: rejectSuggestion (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=False,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_reject_suggestion", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_reject_suggestion(
    service: Any,
    user_google_email: str,
    document_id: str,
    suggestion_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request rejectSuggestion.

    Rejects a suggestion. Returns a 403 forbidden error if the requesting user does not
    have edit access to the document and is not the author of the suggestion.

    Auto-generated wrapper that sends a single RejectSuggestionRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        suggestion_id (str): The ID of the suggestion.

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
    """
    request_body: dict[str, Any] = {}
    if suggestion_id is not None:
        request_body["suggestionId"] = suggestion_id
    body: dict[str, Any] = {"requests": [{"rejectSuggestion": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)


@server.tool(
    title="Docs API Preview: updateCommentPost (batchUpdate)",
    annotations=ToolAnnotations(
        readOnlyHint=False,
        destructiveHint=False,
        idempotentHint=False,
        openWorldHint=True,
    ),
)
@handle_http_errors("docs_api_update_comment_post", service_type="docs")
@require_google_service("docs", "docs_write")
async def docs_api_update_comment_post(
    service: Any,
    user_google_email: str,
    document_id: str,
    comment_id: Optional[str] = None,
    content: Optional[str] = None,
    post_id: Optional[str] = None,
    suggestion_id: Optional[str] = None,
) -> str:
    """[DEVELOPER PREVIEW] Docs API batchUpdate request updateCommentPost.

    Updates a Post in a CommentThread or SuggestionThread. Returns a 400 bad request
    error if the post is the headPost of a SuggestionThread, if the requesting user is
    not the author of the post, or if there are multiple UpdateCommentPostRequests in
    the same BatchUpdateDocumentRequest for the same postId. Exactly one of commentId or
    suggestionId must be set (thread_id union).

    Auto-generated wrapper that sends a single UpdateCommentPostRequest via POST
    v1/documents/{documentId}:batchUpdate.

    Google Workspace Developer Preview: requires preview program enrollment; without it
    the API rejects this request type. Thread/suggestion updates can partially fail even
    when the batch succeeds; check commentUpdateState in the response.

    Args:
        user_google_email (str): The user's Google email address. Required.
        document_id (str): The ID of the document to update.
        comment_id (str): The ID of the CommentThread which the post belongs to
            (thread_id union member).
        content (str): The new text of the comment, as plain text.
        post_id (str): The ID of the post being updated.
        suggestion_id (str): The ID of the SuggestionThread which the post belongs to
            (thread_id union member).

    Returns:
        str: JSON-serialized BatchUpdateDocumentResponse. Fields: commentUpdateState
            (string): Whether comment updates were applied in the batch request.;
            documentId (string); replies (array<Response>): The reply of the updates.;
            suggestionResponses (array<SuggestionResponse>): The suggestions which were
            affected by each update.; writeControl (WriteControl).
    """
    request_body: dict[str, Any] = {}
    if comment_id is not None:
        request_body["commentId"] = comment_id
    if content is not None:
        request_body["content"] = content
    if post_id is not None:
        request_body["postId"] = post_id
    if suggestion_id is not None:
        request_body["suggestionId"] = suggestion_id
    body: dict[str, Any] = {"requests": [{"updateCommentPost": request_body}]}
    api_call = service.documents().batchUpdate(documentId=document_id, body=body)
    response = await asyncio.to_thread(api_call.execute)
    if not response:
        return "Success: the API returned an empty response."
    return json.dumps(response, indent=2, sort_keys=True)
